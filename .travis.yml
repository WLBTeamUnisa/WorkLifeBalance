language: java

jdk:
  - openjdk8

sudo: false

cache:
  timeout: 1000
  bundler: true
  directories:
    - ${HOME}/.m2

before_install:
  - mvn checkstyle:check

script: | 
    mvn test
  
script: "mvn cobertura:cobertura"

after_success:
  - bash <(curl -s https://codecov.io/bash)

notifications: never
